import { observer } from 'mobx-react-lite';
import React from 'react'

const ResellerContainer = () => {
    return (
        <div>
            
        </div>
    )
}

export default observer(ResellerContainer);